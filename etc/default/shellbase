# shellbase system environment configuration (tracked template)
#
# DO NOT EDIT THIS FILE directly - it will be overwritten on git pull.
#
# For personal customizations, copy to ~/.shellbase-system.env and modify there:
#   cp $SHELLBASE_REPO_DIR/etc/default/shellbase ~/.shellbase-system.env
#
# Variables set here are used by shellbase scripts to avoid hardcoded paths.

# --- Core User Identity ---
export SHELLBASE_USER="${SHELLBASE_USER:-$(id -un)}"
export SHELLBASE_USER_HOME="${SHELLBASE_USER_HOME:-$HOME}"

# --- Project Directories ---
export SHELLBASE_PROJECT_ROOT="${SHELLBASE_PROJECT_ROOT:-$HOME/IdeaProjects}"
export SHELLBASE_REPO_DIR="${SHELLBASE_REPO_DIR:-$SHELLBASE_PROJECT_ROOT/shellbase}"

# --- Script Directories ---
export SHELLBASE_BIN_DIR="${SHELLBASE_BIN_DIR:-$HOME/bin}"
export SHELLBASE_REPO_BIN_DIR="${SHELLBASE_REPO_BIN_DIR:-$SHELLBASE_PROJECT_ROOT/shellbase/bin}"

# --- Data Directories ---
export SHELLBASE_BACKUP_DIR="${SHELLBASE_BACKUP_DIR:-$HOME/Documents/system-info}"
export SHELLBASE_CACHE_DIR="${SHELLBASE_CACHE_DIR:-$HOME/.cache}"
export SHELLBASE_CONFIG_DIR="${SHELLBASE_CONFIG_DIR:-$HOME/.config}"

# --- Cloud Sync (Optional) ---
# Uncomment and customize for your cloud provider in ~/.shellbase-system.env:
# export SHELLBASE_CLOUD_DIR="$HOME/Dropbox"
# export SHELLBASE_CLOUD_PASSWORD_FILE="$HOME/.ssh/cloud_backup.pw"

# --- Source Tracking (for debugging) ---
export SHELLBASE_ENV_SOURCE="template"
